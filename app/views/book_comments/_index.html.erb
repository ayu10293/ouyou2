<table>
  <tbody>
    <% book.book_comments.each do |book_comments| %>
    <tr>
      <td>
        <%= link_to user_path(book_comments.user) do %>
          <%= image_tag book_comments.user.get_profile_image, size:'50x50' %>
          <%= book_comment.user.name %>
        <% end %>
      </td>
      <td>
        <%= book_comment.comment %>
      </td>
      <td>
        <% if book_comment.user == current_user %>
          <%= link_to "Destroy", book_book_comment_path(book.id), method: :delete, "data-confirm" => "削除しますか？" %>
        <% end %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>